# Pre-Flight: v88_referral_agents_public_directory_optin

**Migration**: v88_referral_agents_public_directory_optin.sql
**Date**: 2026-07-03
**Author**: run-work F-22 sub-agent (session rw-86e1h5j3x-f22-a015)
**D-numbers**: D-182 (Tier 3), D-221 (Path A)
**ClickUp task**: 86e1h5j3x (SEO P2 — /partners/ referral-partner directory)
**Status**: DRAFT ONLY — NOT APPLIED. Requires a D-182 approval task before execution.

---

## Change Summary

Adds a `public_directory_optin` boolean column to `public.referral_agents` with `NOT NULL DEFAULT false`. This column gates which referral partners appear in the public `/partners/` SEO directory (generated by `tools/generate_partner_pages.py`). All existing agents default to opted out (`false`). Agents must explicitly opt in (opt-in UI is a separate build task). Additive only; no data loss risk.

Until this migration is applied, the generator detects the missing column and exits cleanly with zero pages ("column pending migration v88" notice) — verified in the PR's generator run output.

---

## Row Count Estimate

| Table | Row Count | Source |
|-------|-----------|--------|
| referral_agents | 2 | Orchestrator live-DB dup-check — 2026-07-03 |

---

## Lock Duration Estimate

| Operation | Lock Type | Estimated Duration |
|-----------|-----------|-------------------|
| ADD COLUMN with DEFAULT (2 rows) | ACCESS EXCLUSIVE (brief) | < 5ms — negligible |

---

## Danger Pattern Check

| # | Pattern | Triggered? | Override? |
|---|---------|-----------|-----------|
| 1 | NOT NULL column without DEFAULT | ✅ No — has `DEFAULT false` | — |
| 2 | NOT NULL on table > 100K rows | ✅ No — only 2 rows | — |
| 3 | DROP COLUMN | ✅ No | — |
| 4 | Type change requiring table rewrite | ✅ No | — |
| 5 | Index without CONCURRENTLY on hot table | ✅ No — no index added | — |
| 6 | RENAME TABLE or RENAME COLUMN | ✅ No | — |
| 7 | TRUNCATE or DELETE all rows | ✅ No | — |
| 8 | CASCADE DROP | ✅ No | — |

**All 8 patterns clear. No overrides required.**

---

## Code Path Impact Analysis

- **Existing reads of `referral_agents`**: No code path references `public_directory_optin` today except the new generator, which probes for the column and degrades gracefully when absent. Adding a column with a server-side DEFAULT is fully backward-compatible.
- **`referral_agents_public` view (sql/v88-referral-agents-public-view.sql)**: The view enumerates its columns explicitly, so the new column is NOT exposed through it. No view change required for this migration; the generator reads the base table with the service role key.
- **RLS policies**: No change required. The column follows the same row-level access rules as the rest of the table.
- **No index added**: Boolean opt-in on a 2-row table does not warrant an index. Revisit if the directory grows.

---

## Numbering Collision Note

`sql/v88-referral-agents-public-view.sql` (2026-06-13, legacy `sql/` lineage) also carries the v88 label. This migration follows the `supabase/migrations/` lineage (v83–v87 present there). **At approval time, confirm whether the lineages have been unified and renumber this file to v89 if so.**

---

## Supabase Branch Test Results

**Not run.** This migration is draft-only per the work order (no DB writes of any kind, including branch tests). Run a branch test as part of the D-182 approval workflow before applying.

---

## Deploy Notes

- **D-182 Tier**: 3 — SQL migration. Requires explicit Dustin approval before execution. Approval task to be filed by the run-work orchestrator at closure.
- **D-221 Deploy Path**: GitHub PR → merge → approved manual apply (Path A). Do NOT execute via direct Supabase push or bash. Merging the PR does NOT apply this migration — there is no auto-run pipeline for supabase/migrations in this repo.
- **Rollback pre-authorized**: Yes — run `v88_..._rollback.sql` if schema errors appear post-apply. Rollback destroys any opt-in data written after the forward migration runs.
- **Monitoring**: Watch Sentry for 30 minutes post-apply for unexpected DB errors on `referral_agents`.
- **Follow-on work**: (1) Opt-in UI toggle for partners (partner-dashboard.html) — separate task. (2) First real generator run + PR with generated pages once the column lands and agents opt in.

---

## Danger Overrides

None.
