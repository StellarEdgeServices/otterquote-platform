# Secret env — contains Supabase service role key, NEVER commit
.env.test

# Runtime state written by seed.mjs — contains test account UUIDs and claim ID
.test-state.json

# Playwright artifacts
playwright-report/
test-results/

# Node
node_modules/
dist/
