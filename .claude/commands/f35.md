Read the Wingman F-35 protocol and execute it end to end.

When invoked, read `C:\Users\Dustin Stohler\Downloads\Claude Downloads\Claude's Memories\Skills\wingman-code\SKILL.md` and execute it as **Wingman F-35 (Opus tier)**.

Pull eligible Tier-1 Opus-tagged tasks from ClickUp list 901711730553 matching the F-35 tier. Execute autonomously within Tier A/B authority. Claim tasks via comment-ordering, write a heartbeat to `In Flight/heartbeat/`, write done files on completion, and write a shift-log shard + handoff file at session end.

F-35 executes on the Opus model. For a true Opus run interactively, invoke this in an Opus Code session. This is the deterministic equivalent of the bare-word `F35` trigger. Use `/f35` if the bare word does not reliably load the skill.

$ARGUMENTS
