Read the Wingman F-22 protocol and execute it end to end.

When invoked, read `C:\Users\Dustin Stohler\Downloads\Claude Downloads\Claude's Memories\Skills\wingman-code\SKILL.md` and execute it as **Wingman F-22 (Sonnet tier)**.

Pull eligible Tier-1 Sonnet/unassigned tasks from ClickUp list 901711730553 matching the F-22 tier. Execute autonomously within Tier A/B authority. Claim tasks via comment-ordering, write a heartbeat to `In Flight/heartbeat/`, write done files on completion, and write a shift-log shard + handoff file at session end.

This is the deterministic equivalent of the bare-word `F22` trigger. Use `/f22` if the bare word does not reliably load the skill.

$ARGUMENTS
