# FIXTURE (bad): a named repository secret in a stranger-triggered job.
# (The `run:` is a block scalar because the inline form of this command is not
# valid YAML -- caught by the parser rewrite: an unparseable workflow now
# reports UNPARSEABLE and fails closed rather than being judged on regexes.)
name: prt named secret (bad)
on:
  pull_request_target:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - env:
          KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          curl -H "apikey: $KEY" https://example.invalid
